import { createTheme } from '@mui/material'

const theme = createTheme({
  palette: {
    primary: {
      main: '#000000',
    },
    secondary: {
      main: '#0F0F0F',
    },
    tosei: {
      main: 'pink',
    },
  },
})

export default theme
